# Additional original TXT inputs

This local folder is ignored by Git except for this note. The archived notebook expects `line-profiles/25.txt` through `200.txt`, `lenses/25.4mm.txt`, `100mm.txt`, `200mm.txt`, and `expander/Before.txt`, `After.txt`. Those inputs were not supplied. The runnable portfolio analysis uses the shared XLSX files in `../measurements/`.
