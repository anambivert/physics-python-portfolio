# Sample inputs

This project includes the complete supplied measurements in [../measurements/](../measurements/), so no reduced sample dataset is required. Numerical tests generate synthetic inputs in temporary directories.
